export function fechaGuatemala() { const d = new Date(); const offset = d.getTimezoneOffset() + 360; const local = new Date(d.getTime() - (offset * 60000)); return local.toISOString().slice(0, 10); } export function fechaHoraGuatemala() { const d = new Date(); const offset = d.getTimezoneOffset() + 360; const local = new Date(d.getTime() - (offset * 60000)); return local.toISOString().slice(0, 19).replace('T', ' '); }
